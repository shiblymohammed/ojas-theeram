"use client";

// The entire HowItWorks tracking and functionality has been strategically merged into 
// ConditionsSection.tsx to share the exact same background wrapper and scroll-context!
// This component returns null to prevent layout breaks in page.tsx while keeping file structure clean.

export default function HowItWorksSection() {
  return null;
}
